version: '3.8'

services:
  pg-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pg-mcp
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - LOG_LEVEL=INFO
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - pg-mcp-network

networks:
  pg-mcp-network:
    driver: bridge
